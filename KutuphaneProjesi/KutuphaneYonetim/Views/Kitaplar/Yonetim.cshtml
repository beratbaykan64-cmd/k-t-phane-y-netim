@model IEnumerable<KutuphaneYonetim.Models.Kitap>

@{
    ViewData["Title"] = "Yönetim Paneli";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>⚙️ Yönetim Paneli</h2>
        <a asp-action="Ekle" class="btn btn-success">➕ Yeni Kitap Ekle</a>
    </div>

    <div class="alert alert-info">
        Buradan kitapları düzenleyebilir veya silebilirsiniz.
    </div>

    <table class="table table-bordered table-hover">
        <thead class="table-dark">
            <tr>
                <th>Kitap Adı</th>
                <th>Yazar</th>
                <th>Sayfa</th>
                <th>İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model) {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Ad)</td>
                    <td>@Html.DisplayFor(modelItem => item.Yazar)</td>
                    <td>@Html.DisplayFor(modelItem => item.SayfaSayisi)</td>
                    <td>
                        <a asp-action="Guncelle" asp-route-id="@item.Id" class="btn btn-warning btn-sm">✏️ Düzenle</a>
                        <a asp-action="Sil" asp-route-id="@item.Id" class="btn btn-danger btn-sm" onclick="return confirm('Silmek istediğine emin misin?');">🗑️ Sil</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>